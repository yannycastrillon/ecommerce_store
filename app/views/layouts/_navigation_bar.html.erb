<div class="navbar-component">
  <nav class="navbar navbar-inverse navbar-fixed-top principal-nav-bar">
    <div class="col-md-1">
    </div>
    <div class="col-md-11">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar" {{ action "toggleMenu" }}>
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>
      <div id="navbar" class="collapse navbar-collapse" style="margin-top: 0.8em;">
        <%= form_tag products_path, method: :get do  %>
        <ul class="nav navbar-nav">
          <% if user_signed_in? %>
            <li><%= link_to "Logged in as #{ current_user.email}",user_path(current_user) , styles:"color:blue;"%> | </li>
            <li><%= link_to 'Home', home_path%> | </li>
            <li><%= link_to 'Edit Profile', edit_user_registration_path %> | </li>
            <li><%= link_to 'My orders', user_orders_path(current_user) %> | </li>
            <li><%= link_to 'My Cart', cart_path %> | </li>
            <li><%= link_to 'Sign out', destroy_user_session_path, method: :delete %> | </li>
            <li><%= text_field_tag :search_term, "", placeholder:"Ex: Products, Categories", class:"form-control", style:"margin-top:0.5em;" %> </li>
            <li><%= submit_tag "Search", :name => nil, class:"btn btn-warning", style:"margin-top:0.5em;"%> | </li>
          <% end %>
        <% if user_signed_in? %>
              <% if current_user.present? and current_user.admin?  %>
                <li><%= link_to 'All Users', users_path  %> | </li>
                <li><%= link_to 'All Categories', categories_path %> | </li>
              <% end %>
        <% else %>
          <li><%= link_to 'Sign up', new_user_registration_path %> | </li>
          <li><%= link_to 'Sign in', new_user_session_path %> | </li>
        <% end %>
      </ul>
      <% end %>
    </div>
  </nav>
</div>
